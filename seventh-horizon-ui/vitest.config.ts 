import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
    exclude: ['tests/**', 'node_modules/**', 'dist/**'],
    restoreMocks: true,
    setupFiles: ['src/test/setup-vitest.ts'], // ← add this line
  },
});